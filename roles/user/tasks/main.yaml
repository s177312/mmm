---
- name: Install sudo
  ansible.builtin.apt:
    name: sudo
    state: present
  become: true

- name: Manage user
  ansible.builtin.user:
    name: "{{ ansible_facts.user_id }}"
    groups: sudo
    append: true
  become: true
