---
apt:
  setup_file: /etc/.ansible_apt_setup
  packages:
    - apt-transport-https
    - bash
    - curl
    - wget
    - git
    - gnome-keyring
    - gpg
    - gcc
    - make
    - sudo
    - python3
    - python3-pip
user:
  name: silk
